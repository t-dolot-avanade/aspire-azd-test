{
    "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
      "principalId": {
        "value": "${AZURE_PRINCIPAL_ID}"
      },
      "blobs": {
        "value": "${AZURE_BLOBS}"
      },
      "local_sql_password": {
        "value": "${AZURE_LOCAL_SQL_PASSWORD}"
      },
      "local_sql_port": {
        "value": "${AZURE_LOCAL_SQL_PORT}"
      },
      "secrets": {
        "value": "${AZURE_SECRETS}"
      },
      "environmentName": {
        "value": "${AZURE_ENV_NAME}"
      },
      "location": {
        "value": "${AZURE_LOCATION}"
      }
    }
  }
  